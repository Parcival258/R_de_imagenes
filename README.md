# Microservicio IA - Comprobantes

Microservicio de FastAPI con OCR usando PaddleOCR para analizar comprobantes de pago.

## Instalación

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt



#arrancar el micro servicio con 
# primero con "venv\Scripts\activate"
#desoues con "uvicorn app.main:app --reload --host 0.0.0.0 --port 8001"
